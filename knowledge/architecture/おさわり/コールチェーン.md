## おさわり コールチェーン

Unity入力からドメイン処理までの完全なコールチェーン:

```
[Unity/Presentation]
Clickable.OnPointerClick(PointerEventData)   // unity/Assets/Scripts/Presentation/Clickable.cs:73
  → Clickable.SendInput(InteractionType)      // 同ファイル:114
  → PartController.SendInput(PartInput)       // core/Zrushy.Core.Presentation/PartController.cs:24
      ├→ InteractPart.Execute(InteractPartCommand)  // core/Zrushy.Core.Application/UseCase/InteractPart/InteractPart.cs:22
      │     → Heroin.Interact(Interaction)          // core/Zrushy.Core.Domain/Interactions/Entity/Heroin.cs:58
      │           ├→ Part.CalculateArousal(...)     // core/Zrushy.Core.Domain/Interactions/Entity/Part.cs:31
      │           ├→ Part.Interact(Interaction)     // core/Zrushy.Core.Domain/Interactions/Entity/Part.cs:25
      │           ├→ IEventEvaluator.Evaluate(Interaction)  // → EventEvaluator.cs:29
      │           │     ├→ interactionHistory.Record(interaction)
      │           │     ├→ eventRepository.GetEvents(PartID) + GetGlobalEvents()
      │           │     ├→ IScenarioEvent.CanFire() でフィルタ・Priority でソート
      │           │     └→ eventBus.Publish(fired)  // OnEventPublished?.Invoke() でシナリオ実行へ
      │           └→ ApplyCooldown()  // クリマックス時（Arousal >= 100）のみ
      └→ ScenarioInputGate.NotifyTouch(partId)  // wait_for_touch の解放
```
