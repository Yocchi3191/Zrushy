## Action-Event-Scenario 実装

オブジェクトの役割感がまったく湧いてないので、UseCase から組んでく

## 2026.01.31(Sat)

シナリオ送りの設計が全然うまいことわかねー

- さわり

```mermaid
sequenceDiagram

Clickable ->> PartController : SendInput(input)
PartController ->> InteractPart : Execute(command)
InteractPart ->> Body : Interact(interaction)
Body ->> Part : Interact(?)
```

- イベント取得

```mermaid
sequenceDiagram

InteractPart ->> IEventRepository : GetEvents(partID)
IEventRepository -->> InteractPart : events
InteractPart -->> PartController : InteractPartResult
```

```mermaid
sequenceDiagram
    participant User
    participant Clickable
    participant PC as PartController
    participant IP as InteractPart
    participant ER as EventRepository
    participant SP as ScenarioPlayer
    participant SE as ScenarioEngine
    participant VM as ViewModel

    User->>Clickable: タッチ(Chest)
    Clickable->>PC: SendInput(partID: Chest)

    rect rgb(200, 220, 255)
        Note over PC,ER: Phase 1: 通常インタラクション
        PC->>IP: Execute(command)
        IP->>IP: Body.Interact(interaction)
        Note right of IP: Development+5<br/>Pleasure+3
        IP->>ER: GetEvents(partID: Chest)
        ER-->>IP: [FirstTouchEvent]
        IP-->>PC: InteractPartResult(firedEvent: FirstTouchEvent)
    end

    rect rgb(255, 220, 200)
        Note over PC,VM: Phase 2: シナリオ開始
        PC->>SP: Start(scenarioID: "chest_first")
        SP->>SE: Start("chest_first")
        SE-->>SP: OK

        loop シナリオ命令を順次実行
            SP->>SE: GetNext()

            alt ShowAction命令
                SE-->>SP: ShowAction(action)
                SP->>VM: UpdateAction(action)
                Note right of VM: セリフ・表情・アニメを表示
                Note over SP: すぐ次の命令へ

            else AwaitTouch命令
                SE-->>SP: AwaitTouch(partID: Chest)
                SP->>VM: SetAwaitingInput(partID: Chest)
                Note right of VM: 「もっと触って」UI表示
                Note over SP: ここで一時停止
                SP-->>PC: (制御を返す)
                Note over PC,SP: ScenarioPlayer.IsRunning = true<br/>で実行中状態を保持
            end
        end
    end

    User->>Clickable: 再度タッチ(Chest)
    Clickable->>PC: SendInput(partID: Chest)

    rect rgb(200, 255, 200)
        Note over PC,VM: Phase 3: シナリオ再開
        alt ScenarioPlayer.IsRunning == true
            PC->>SP: ProvideInput(partID: Chest)
            SP->>SE: ProvideInput(Chest)

            SP->>SE: GetNext()
            SE-->>SP: ShowAction(action2)
            SP->>VM: UpdateAction(action2)

            SP->>SE: GetNext()
            SE-->>SP: ScenarioEnd
            Note over SP: シナリオ完了
            SP->>SP: IsRunning = false
            SP-->>PC: (完了通知)
        end
    end
```

```mermaid
graph LR
    A[PartController] -->|Execute| B[InteractPart]
    B -->|Result| A
    A -->|Play| C[ScenarioPlayer]
    C -->|Start/Next| D[IScenarioEngine]
    D -->|Action| C
    C -->|Act| E[HeroinViewModel]
    E -->|OnUpdated| F[HeroinView]

    style C fill:#ffcccc
    style E fill:#ccccff

```

## 2026.02.01(Sun)

```mermaid
  classDiagram

  namespace Presentation.Unity{
      class Heroin
      class Part
      class Clickable
      class ScenarioDriver
      class HeroinView
  }

  namespace Presentation{
      class PartController
      class ScenarioPlayer
      class HeroinViewModel
  }

  namespace Domain{
      class IScenarioEngine
      class Action
      class IEvent
  }

  Heroin "1" *-- "*" Part
  Heroin "1" *-- "1" ScenarioDriver
  Heroin "1" *-- "1" HeroinView

  Part "1" *-- "1" Clickable

  Clickable --> PartController : SendInput(PartInput)
  PartController --> ScenarioPlayer : Play(ScenarioID)
  ScenarioDriver --> ScenarioPlayer : Next()
  ScenarioPlayer --> IScenarioEngine : Start / GetNext
  ScenarioPlayer --> HeroinViewModel : UpdateAction(Action)
  HeroinView --> HeroinViewModel : listen
```

`ScenarioPlayer.Play(Scenario)`を internal にすれば、Unity から触られることはない
`ScenarioDriver`の名前は後で決めるとして、こいつが常に Update して`ScenarioPlayer.Next()`を叩くことで、ViewModel を更新する
`ViewModel --> View`のデータ送信は従来の`OnUpdated`イベント起動でいける

### claude からの質問

1. ScenarioDriver: 条件を満たさないときは早期 return
2. ScenarioPlayer: Next()で Scenario の次の Action をもらってくる
3. シナリオ的に分岐はある

### claude

1. シナリオの話の進行部分に yarn spinner を使おうと思ってるんですが、3 の解決に役立つことはありますか？
2. Unity 側のオブジェクト配置で忘れていましたが、`Heroin *-- Part` である必要がありました。Action によるアセット操作の範囲は、パーツ単位ではなくキャラクターまるまる 1 人分で考えています。もっといい変更の範囲があれば提案してください
    1. となると ScenarioPlayer の操作単位を ViewModel にするのは不適切かもしれません。でも Heroin 全体を操作できるオブジェクトは現状ないんですよね。Domain 側に Body はあるんですが

```mermaid
  sequenceDiagram
      participant C as Clickable
      participant PC as PartController
      participant IP as InteractPart
      participant SP as ScenarioPlayer
      participant SE as IScenarioEngine
      participant PVM as PartViewModel
      participant HVM as HeroinViewModel
      participant SD as ScenarioDriver

      C->>PC: SendInput(Chest)
      Note over PC: IsPlaying == false → 通常処理

      PC->>IP: Execute(command)
      IP-->>PC: result(FiredEvent)

      PC->>PVM: Update(result)

      PC->>SP: Play(event.ScenarioToStart)
      SP->>SE: Start(scenarioID)
      SP->>SE: GetNext()
      SE-->>SP: ShowAction(action1)
      SP->>HVM: UpdateAction(action1)
      Note over SP: 停止。ScenarioDriverを待つ

      SD->>SP: Next()
      SP->>SE: GetNext()
      SE-->>SP: ShowAction(action2)
      SP->>HVM: UpdateAction(action2)

      SD->>SP: Next()
      SP->>SE: GetNext()
      SE-->>SP: ScenarioEnd
      Note over SP: IsPlaying = false

```

### シーン起動修正の実装

クリック → シナリオ再生の一連のフローが動く状態にするための実装を行った。

**問題点:**

- `IScenarioEngine`, `HeroinViewModel` の Zenject バインドがなく ScenarioPlayer 生成失敗
- `IScenarioEngine` の実装クラスが存在しない
- `EventRepository` が空リストを返し `UndefinedReactionException` 発生
- `HeroinView`, `ScenarioDriver` の MonoBehaviour が未作成

**実装内容:**

| ファイル                                       | 操作 | 内容                                                                          |
| ---------------------------------------------- | ---- | ----------------------------------------------------------------------------- |
| `Infrastructure/Engine/ListScenarioEngine.cs`  | 新規 | IScenarioEngine 実装。ScenarioID に対応する固定 Action 列を返す               |
| `Infrastructure/Repository/EventRepository.cs` | 変更 | DefaultEvent（CanFire 常に true）を返すように変更                             |
| `DI/ZrushyInstaller.cs`                        | 変更 | IScenarioEngine→ListScenarioEngine, HeroinViewModel バインド追加              |
| `Presentation/HeroinView.cs`                   | 新規 | HeroinViewModel 購読、Debug.Log でセリフ出力                                  |
| `Presentation/ScenarioDriver.cs`               | 新規 | InputSystem の Mouse.current.leftButton.wasPressedThisFrame で Next()呼び出し |

**結果:**

- dotnet test 11/11 パス
- Unity シーン起動 → パーツクリック → Debug.Log にセリフ表示 → クリックで Next → 次のセリフ表示 まで動作確認済み

### 残タスク

- シナリオの分岐機能の実装
- シナリオ、イベントのマスタ化
    - もっと整備しやすく、設定しやすくしたい
    - yarn spinner 使えばどっちもなんとかなるらしい。実際に触るときに確認しよう
- イベントの実装
- アクション(見た目変化)の実装
    - 見た目変化するってことは Clickable の位置とか調整できないとだめじゃね
